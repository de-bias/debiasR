% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjust_selection_rate2.R
\name{adjust_selection_rate2}
\alias{adjust_selection_rate2}
\title{Selection Rate II weighting (Zagheni & Weber 2012) with k calibration}
\usage{
adjust_selection_rate2(
  mpd_od_df,
  coverage_df,
  weight_by = c("origin", "destination", "both"),
  group_cols = NULL,
  k = NULL,
  k_grid = seq(0.1, 5, by = 0.1),
  benchmark_od_df = NULL,
  flow_col_bench = "flow",
  calibration_aggregate = c("origin", "od"),
  clip_min = 0,
  clip_max = Inf,
  keep_cols = character()
)
}
\arguments{
\item{mpd_od_df}{Data frame with at least:
origin, destination, flow, mpd_source; plus group_cols if used.}

\item{coverage_df}{Data frame with:
NEW: origin, origin_population, origin_user_count,
destination, destination_population, destination_user_count, mpd_source
or LEGACY: origin, population, user_count, mpd_source.
Must contain group_cols if used.}

\item{weight_by}{"origin", "destination", or "both".}

\item{group_cols}{Optional character vector of stratification variables
present in both mpd_od_df and coverage_df (e.g. c("age_group","sex")).}

\item{k}{Optional positive scalar. If NULL and benchmark_od_df supplied,
k is calibrated by grid search. If NULL and no benchmark, k = 1.}

\item{k_grid}{Grid of candidate k values for calibration when k is NULL.
Default seq(0.1, 5, by = 0.1).}

\item{benchmark_od_df}{Optional benchmark OD for calibrating k.
Must contain origin, destination, and a flow column.}

\item{flow_col_bench}{Name of benchmark flow column. Default "flow".}

\item{calibration_aggregate}{"origin" (default, compare origin totals)
or "od" (compare OD flows directly).}

\item{clip_min}{Lower bound used to clamp weights. Default 0.}

\item{clip_max}{Upper bound used to clamp weights. Default Inf.}

\item{keep_cols}{Extra columns from mpd_od_df to retain.}
}
\value{
Tibble with:
origin, destination, mpd_source, (group_cols), flow,
weight_origin, weight_destination, weight_missing, flow_adj.
Attributes:
- "k" : numeric k used.
- "k_calibration" : data.frame of k vs loss (if calibrated).
}
\description{
Notation used throughout:
\itemize{
\item \eqn{P_i^{(O)}, P_j^{(D)}}: population at origin \eqn{i} and destination \eqn{j}
\item \eqn{U_i^{(O)}, U_j^{(D)}}: active users at origin \eqn{i} and destination \eqn{j}
\item \eqn{p_i^{(O)} = U_i^{(O)}/P_i^{(O)}} and \eqn{p_j^{(D)} = U_j^{(D)}/P_j^{(D)}}: penetration
\item \eqn{F_{ij}^{mpd}} and \eqn{F_{ij}^{adj}}: observed and adjusted flows
\item \eqn{k > 0}: selection-rate curvature parameter
}
}
\details{
Implements the "Selection Rate II" correction:
\deqn{CF(p; k) = p \frac{e^{-k} - 1}{e^{-k p} - 1}}

where \eqn{p} is a penetration rate between 0 and 1 (e.g. Internet or platform
penetration), and k > 0 controls how strongly selection bias increases
as p decreases.

For OD flows:
\itemize{
\item \code{weight_by = "origin"}: \eqn{w_{ij} = CF(p_i^{(O)}; k)}
\item \code{weight_by = "destination"}: \eqn{w_{ij} = CF(p_j^{(D)}; k)}
\item \code{weight_by = "both"}: \eqn{w_{ij} = \sqrt{CF(p_i^{(O)}; k)\,CF(p_j^{(D)}; k)}}
}

Adjusted flows are:
\deqn{F_{ij}^{adj} = F_{ij}^{mpd} \times w_{ij}}

Supports:
\enumerate{
\item Location-only OD (default).
\item Stratified OD (e.g. age/sex) via group_cols; then p and CF are
computed by (area, group_cols).
}

Calibration of k:
\itemize{
\item If k is provided (numeric scalar): use it.
\item Else if benchmark_od_df is provided:
search k_grid, pick k* minimising sum of absolute errors between
adjusted and benchmark flows (origin-aggregated or OD-level).
\item Else: default k = 1.
}
}
